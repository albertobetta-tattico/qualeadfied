# Backend API base. Default per dev locale (php artisan serve sul mac).
NUXT_PUBLIC_API_BASE=http://localhost:8000/api

# Stripe pubblica (test mode). Recupera dal dashboard Stripe del progetto.
NUXT_PUBLIC_STRIPE_PUBLIC_KEY=

# Node env (Nuxt setta da sé in dev/build)
NODE_ENV=development
